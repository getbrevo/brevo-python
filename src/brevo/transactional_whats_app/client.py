# This file was auto-generated by Fern from our API Definition.

import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.request_options import RequestOptions
from .raw_client import AsyncRawTransactionalWhatsAppClient, RawTransactionalWhatsAppClient
from .types.get_whatsapp_event_report_request_event import GetWhatsappEventReportRequestEvent
from .types.get_whatsapp_event_report_request_sort import GetWhatsappEventReportRequestSort
from .types.get_whatsapp_event_report_response import GetWhatsappEventReportResponse
from .types.send_whatsapp_message_request import SendWhatsappMessageRequest
from .types.send_whatsapp_message_response import SendWhatsappMessageResponse

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class TransactionalWhatsAppClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawTransactionalWhatsAppClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawTransactionalWhatsAppClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawTransactionalWhatsAppClient
        """
        return self._raw_client

    def send_whatsapp_message(
        self, *, request: SendWhatsappMessageRequest, request_options: typing.Optional[RequestOptions] = None
    ) -> SendWhatsappMessageResponse:
        """
        <Note>You can use this API for WhatsApp only if you have setup your WhatsApp account on Brevo platform. To setup your WhatsApp account, follow the steps in the guide below. [Activating Whatsapp](https://developers.brevo.com/docs/whatsapp-campaigns-1) in your account</Note>
        This endpoint is used to send a WhatsApp message. <br/>(**The first message you send using the API must contain a Template ID. You must create a template on WhatsApp on the Brevo platform to fetch the Template ID.**)

        Parameters
        ----------
        request : SendWhatsappMessageRequest

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SendWhatsappMessageResponse
            successfully created

        Examples
        --------
        from brevo import Brevo
        from brevo.transactional_whats_app import SendWhatsappMessageRequestParams

        client = Brevo(
            api_key="YOUR_API_KEY",
        )
        client.transactional_whats_app.send_whatsapp_message(
            request=SendWhatsappMessageRequestParams(
                contact_numbers=["contactNumbers"],
                sender_number="senderNumber",
                template_id=123,
            ),
        )
        """
        _response = self._raw_client.send_whatsapp_message(request=request, request_options=request_options)
        return _response.data

    def get_whatsapp_event_report(
        self,
        *,
        limit: typing.Optional[int] = None,
        offset: typing.Optional[int] = None,
        start_date: typing.Optional[str] = None,
        end_date: typing.Optional[str] = None,
        days: typing.Optional[int] = None,
        contact_number: typing.Optional[str] = None,
        event: typing.Optional[GetWhatsappEventReportRequestEvent] = None,
        sort: typing.Optional[GetWhatsappEventReportRequestSort] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> GetWhatsappEventReportResponse:
        """
        This endpoint will show the unaggregated statistics for WhatsApp activity (30 days by default if `startDate` and `endDate` or `days` is not passed. The date range can not exceed 90 days)

        Parameters
        ----------
        limit : typing.Optional[int]
            Number limitation for the result returned

        offset : typing.Optional[int]
            Beginning point in the list to retrieve from

        start_date : typing.Optional[str]
            **Mandatory if endDate is used.** Starting date of the report (YYYY-MM-DD). Must be lower than equal to endDate

        end_date : typing.Optional[str]
            **Mandatory if startDate is used.** Ending date of the report (YYYY-MM-DD). Must be greater than equal to startDate

        days : typing.Optional[int]
            Number of days in the past including today (positive integer). _Not compatible with 'startDate' and 'endDate'_

        contact_number : typing.Optional[str]
            Filter results for specific contact (WhatsApp Number with country code. Example, 85264318721)

        event : typing.Optional[GetWhatsappEventReportRequestEvent]
            Filter the report for a specific event type

        sort : typing.Optional[GetWhatsappEventReportRequestSort]
            Sort the results in the ascending/descending order of record creation. Default order is **descending** if `sort` is not passed

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        GetWhatsappEventReportResponse
            WhatsApp events report

        Examples
        --------
        from brevo import Brevo

        client = Brevo(
            api_key="YOUR_API_KEY",
        )
        client.transactional_whats_app.get_whatsapp_event_report()
        """
        _response = self._raw_client.get_whatsapp_event_report(
            limit=limit,
            offset=offset,
            start_date=start_date,
            end_date=end_date,
            days=days,
            contact_number=contact_number,
            event=event,
            sort=sort,
            request_options=request_options,
        )
        return _response.data


class AsyncTransactionalWhatsAppClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawTransactionalWhatsAppClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawTransactionalWhatsAppClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawTransactionalWhatsAppClient
        """
        return self._raw_client

    async def send_whatsapp_message(
        self, *, request: SendWhatsappMessageRequest, request_options: typing.Optional[RequestOptions] = None
    ) -> SendWhatsappMessageResponse:
        """
        <Note>You can use this API for WhatsApp only if you have setup your WhatsApp account on Brevo platform. To setup your WhatsApp account, follow the steps in the guide below. [Activating Whatsapp](https://developers.brevo.com/docs/whatsapp-campaigns-1) in your account</Note>
        This endpoint is used to send a WhatsApp message. <br/>(**The first message you send using the API must contain a Template ID. You must create a template on WhatsApp on the Brevo platform to fetch the Template ID.**)

        Parameters
        ----------
        request : SendWhatsappMessageRequest

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SendWhatsappMessageResponse
            successfully created

        Examples
        --------
        import asyncio

        from brevo import AsyncBrevo
        from brevo.transactional_whats_app import SendWhatsappMessageRequestParams

        client = AsyncBrevo(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.transactional_whats_app.send_whatsapp_message(
                request=SendWhatsappMessageRequestParams(
                    contact_numbers=["contactNumbers"],
                    sender_number="senderNumber",
                    template_id=123,
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.send_whatsapp_message(request=request, request_options=request_options)
        return _response.data

    async def get_whatsapp_event_report(
        self,
        *,
        limit: typing.Optional[int] = None,
        offset: typing.Optional[int] = None,
        start_date: typing.Optional[str] = None,
        end_date: typing.Optional[str] = None,
        days: typing.Optional[int] = None,
        contact_number: typing.Optional[str] = None,
        event: typing.Optional[GetWhatsappEventReportRequestEvent] = None,
        sort: typing.Optional[GetWhatsappEventReportRequestSort] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> GetWhatsappEventReportResponse:
        """
        This endpoint will show the unaggregated statistics for WhatsApp activity (30 days by default if `startDate` and `endDate` or `days` is not passed. The date range can not exceed 90 days)

        Parameters
        ----------
        limit : typing.Optional[int]
            Number limitation for the result returned

        offset : typing.Optional[int]
            Beginning point in the list to retrieve from

        start_date : typing.Optional[str]
            **Mandatory if endDate is used.** Starting date of the report (YYYY-MM-DD). Must be lower than equal to endDate

        end_date : typing.Optional[str]
            **Mandatory if startDate is used.** Ending date of the report (YYYY-MM-DD). Must be greater than equal to startDate

        days : typing.Optional[int]
            Number of days in the past including today (positive integer). _Not compatible with 'startDate' and 'endDate'_

        contact_number : typing.Optional[str]
            Filter results for specific contact (WhatsApp Number with country code. Example, 85264318721)

        event : typing.Optional[GetWhatsappEventReportRequestEvent]
            Filter the report for a specific event type

        sort : typing.Optional[GetWhatsappEventReportRequestSort]
            Sort the results in the ascending/descending order of record creation. Default order is **descending** if `sort` is not passed

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        GetWhatsappEventReportResponse
            WhatsApp events report

        Examples
        --------
        import asyncio

        from brevo import AsyncBrevo

        client = AsyncBrevo(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.transactional_whats_app.get_whatsapp_event_report()


        asyncio.run(main())
        """
        _response = await self._raw_client.get_whatsapp_event_report(
            limit=limit,
            offset=offset,
            start_date=start_date,
            end_date=end_date,
            days=days,
            contact_number=contact_number,
            event=event,
            sort=sort,
            request_options=request_options,
        )
        return _response.data
